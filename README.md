# vg-be-test-parking-app
CLI-based parking app (automated ticketing system) written in Golang. The system accepts a filename as a parameter at the command prompt and read the commands from that file.
